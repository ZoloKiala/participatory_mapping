web: python manage.py collectstatic --noinput && python manage.py migrate --noinput && if [ -f preloaded_locations.csv ]; then python manage.py load_locations --if-empty; fi && python -m gunicorn pgis_project.wsgi:application --bind 0.0.0.0:${PORT:-8000}
